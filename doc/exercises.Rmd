---
title: "Trying out R Markdown"
author: "Maj Bangshaab"
date: "16/6/2021"
output: word_document
---

1. Maj Bangshaab
2. Aarhus University
3. SDCA Aarhus

# Intro

# Methods and results

## Analysis

This is my **data** analysis *session*.

# Discussion


```{r setup}
source(here::here("R/package-loading.R"))
load(here::here("data/nhanes_small.rda"))
```

```{r prettier-table, echo=FALSE}
nhanes_small %>%
    filter(!is.na(diabetes)) %>% 
    group_by(diabetes, sex) %>% 
    summarise(mean_age = mean(age, na.rm = TRUE),
              mean_bmi = mean(bmi, na.rm = TRUE)) %>% 
    ungroup() %>%
    mutate(mean_age = round(mean_age, 1), 
           mean_bmi = round(mean_bmi, 1),
           sex = str_to_sentence(sex)) %>%
    rename("Diabetes Status" = diabetes, Sex = sex, "Mean Age" = mean_age, "Mean BMI" = mean_bmi) %>% 
    knitr::kable(caption = "A prettier table.")
```
![Alpaka](images/Alpaka.jpg)
